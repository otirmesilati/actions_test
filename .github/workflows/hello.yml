name: Hello Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say hi
        run: echo "Hi! This is my first Github Actions workflow"
